# Xuva Apple Core

Shared SwiftUI client code for the native Xuva iOS and tvOS apps.

App-specific projects live in:

- `apps/tvos`
- `apps/ios`

This package contains media-player client UI, API models, pairing, browsing, detail, and playback flows shared by both Apple targets.
